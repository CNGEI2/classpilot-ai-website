{
  "name": "classpilot-ai-website",
  "version": "1.0.0",
  "private": true,
  "description": "A browser-based academic planner for course syllabi, Canvas assignments, screenshots, and student workplans.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check logic.js && node --check planner.js && node --check file-readers.js && node --check source-evidence.js && node --check submission-checker.js && node --check coach.js && node --check worker/worker.mjs && node --check app.js",
    "verify": "npm test && npm run check",
    "start": "python3 -m http.server 4173 --bind 127.0.0.1"
  },
  "keywords": [
    "education",
    "student-planner",
    "canvas",
    "syllabus",
    "ocr"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "jszip": "3.10.1",
    "lucide": "1.25.0",
    "pdfjs-dist": "6.1.200"
  }
}
